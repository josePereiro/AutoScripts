# >>> osUtils entry >>>
# Redirect to osUtils if not yet done
if [ -z "${__ENTRY_POINT+x}" ]; then 
    # __ENTRY_POINT is unset
    __ENTRY_POINT="/Users/Pereiro/.profile"
    source "/Users/Pereiro/AutoScripts/osUtils/macos/__init__.sh"
fi

# --------------------------------------------------------------


